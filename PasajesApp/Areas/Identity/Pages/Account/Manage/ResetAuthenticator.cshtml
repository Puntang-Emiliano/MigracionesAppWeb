@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Restablecer la clave del autenticador";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Si restableces tu clave del autenticador, tu aplicación de autenticación no funcionará hasta que la reconfigures.</strong>
    </p>
    <p>
        Este proceso deshabilita la autenticación en dos factores (2FA) hasta que verifiques tu aplicación de autenticación.
        Si no completas la configuración de la aplicación de autenticación, podrías perder el acceso a tu cuenta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Restablecer la clave del autenticador</button>
    </form>
</div>
